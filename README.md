
# Onchain Social Hub

Generates **onchain identity**, **reputation score**, and **auto-discovers communities** from wallet holdings using **Zerion API**.

## Features
- No sign-up
- Real-time, multi-chain data
- 100% onchain social graph

## Setup
```bash
